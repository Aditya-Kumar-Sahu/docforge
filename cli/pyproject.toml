[project]
name = "cli"
version = "0.1.0"
description = "DocForge CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.15.2",
    "rich>=14.1.0",
]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "ruff>=0.15.12",
]
